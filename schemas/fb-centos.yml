- src: "{app_name}-{tag}-1.centos-{os_version}.{arch}.rpm"
  arch:
    - x86_64
    - arm64
  uploads:
    - type: yum
      dest: "{dest_prefix}linux/yum/el/{os_version}/{arch}/"
      os_version:
        - 7
        - 8